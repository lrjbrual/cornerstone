        <div class="row">
          <div class="col-md-10">
            <div class="card">
              <div class="card-header">
              <div class="row">
                <div class="col-md-6">
                <h4 class="card-title"><%= @categorytitle %></h4>
                </div>
                <div class="col-md-6 text-right">
                  <%= link_to 'Create New', new_admin_category_path, class: "btn btn-primary btn-sm" %>
                </div>
              <%= render 'shared/alert' %>
              </div>
              </div>
              <div class="card-body">
                <div class="table-responsive">
                  <table class="table">
                  <% if @categories.exists? %>
                    <thead class=" text-primary">
                      <tr><th>
                        Name
                      </th>
                      <th>
                        Date Created
                      </th>
                      <th>
                        Action
                      </th>
                    </tr></thead>
                    <tbody>
                    <% @categories.each do |category| %> 
                      <tr>
                        <td>
                          <%= category.name %>
                        </td>
                        <td>
                          <%= time_ago_in_words(category.created_at) %> ago
                        </td>
                        <td class="text-left">
                          <%= link_to edit_admin_category_path(category) do %>
                          <i class="far fa-lg fa-edit text-primay"></i>
                          <% end %>
                          <%= link_to admin_categories_path(category), method: :destroy, data: {confirm: 'Are you sure?'} do %>
                            <i class="far fa-trash-alt fa-lg text-warning"></i>
                          <% end %>
                        </td>
                      </tr>
                    <% end %>
                    <% else %>
                      <p>There is no category</p>
                    <% end %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>